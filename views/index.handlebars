<img src="assets/img/burger01.jpg" alt="Food" class="responsive">

<h1>Eat-Da-Burger!</h1>

<h2>Choose your burger</h2>

<ul id="burger_area">
  {{#each burgers}}
  {{#unless devoured}}
  <li>{{burger_name}}<br><button class="burger_button" value="{{id}}">Devour</button></li>
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  <li>{{burger_name}}</li>
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-update-form">
  <div class="form-group">
    <textarea class="form-control input-lg" id="newBurger" placeholder="Add a burger & order!" type="text"
      name="burger_name" row="3"></textarea>
  </div>
  <button class="btn btn-default btn-lg" type="submit">Order</button>
</form>